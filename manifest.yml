---
declared-services:
  swift-evolution-auto-scaling:
    label: Auto-Scaling
    plan: free
applications:
- name: swift-evolution-backend
  memory: 512M
  instances: 1
  random-route: true
  disk_quota: 1024M
  command: Server
  buildpack: swift_buildpack
  services:
  - swift-evolution-auto-scaling
